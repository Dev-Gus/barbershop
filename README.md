# Barbero Maykol
Landing page — Barbero Maykol, Tacuarembó, Uruguay.

**Stack:** HTML + Tailwind CSS v4 (CLI) + Vanilla JS.

## Setup
npm install
npm run dev    # watch mode
npm run build  # one-shot build

> `src/output.css` is gitignored. Run `npm run build` after cloning before opening the site.